import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      light: "#f1f1f1",
    },
    secondary: {
      main: "#000",
      light: "#777",
    },
    background: {
      default: "#f1f1f1",
      paper: "#fff",
    },
  },
});