import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      default: "#f1f1f1",
      paper: "#fff"
    },
    primary: {
      light: "#f1f1f1",
      main: "#fff"
    },
    secondary: {
      light: "#777",
      main: "#000"
    }
  }
});
